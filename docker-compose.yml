services:
  autograder:
    build: .
    ports:
      - "5001:5000"
    env_file:
      - .env
    volumes:
      - ./uploads:/app/uploads
      - ./data:/app/data
      - ./.env:/app/.env:ro
      - ./config.yaml:/app/config.yaml:ro
      - ./prompts:/app/prompts:ro
