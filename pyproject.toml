[tool.pytest.ini_options]
pythonpath = ["."]

[tool.ruff]
target-version = "py311"
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = [
    "E501",   # line length — long lines in HTML templates and API URLs
]

[tool.ruff.lint.isort]
known-first-party = ["config", "code_runner", "prompt_loader"]
